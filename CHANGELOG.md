# 更新记录

## Unreleased

### 通用发行与构建配置

- 移除个人部署的默认站点、品牌名称、包名、平台通道名称及签名路径探测；保留上游项目引用和署名。
- 构建命令支持应用名称、包名、简介、默认站点及版本，可使用参数或被 Git 忽略的本地 JSON。
- Flutter 文案和 Android 应用元数据共用构建参数，不必为个人发行版修改源码。
- 未配置站点时可先添加服务器，避免空列表报错；服务器按 URL 保存选择并兼容旧自定义地址。
- 修复新增服务器弹窗在退出动画结束前过早释放输入框的问题。
- 加入构建参数、无预设站点及服务器迁移回归测试。

### 工程规范化

- 统一手写 Dart 文件命名、导入顺序和格式，启用严格的提交前检查。
- 修正异步页面上下文使用，分享创建与结果展示分离。
- 固定 Flutter 工具版本，纳入应用依赖锁文件。
- 支持构建时覆盖默认站点，移除诊断工具对个人 SDK 路径及固定私有测试资源的依赖。
- 隔离本地输出与签名材料，正式发布不再静默使用调试签名。
- 增加 GitHub CI、贡献规范、安全说明、项目来源和发布清单。

当前版本保留已有的文件管理、缓存、账户、主题动画和原生音视频功能。既往未正式标记的开发构建不追溯编造发布日期或版本号。
