# 安全报告与隐私

安全问题请优先使用仓库的 Private vulnerability reporting（若维护者已启用）或私下联系维护者；不要把有效凭据、私人文件或利用细节发到公开 Issue。若没有私密渠道，先询问联系方式，不公开敏感内容。

报告建议包含应用版本、Android / Cloudreve 服务端版本、最小复现、影响范围，以及已脱敏的日志。仅在自有或明确获准的环境验证，不操作其他用户数据。

## 安全边界

- 客户端连接 HTTPS Cloudreve 站点；不得加入跳过证书校验的生产开关。
- 登录凭据、访问/刷新令牌不属于构建配置，不得放入 dart-define、代码或日志。
- 日志应只包含错误类别和必要状态码，不包含 Authorization、Cookie、签名地址和完整服务端响应。
- 图片缓存与请求缓存按会话隔离，账号退出和切换必须失效。
- 正式签名私钥、口令和本地配置必须留在 Git 之外；CI 默认不读取发布密钥。
- `tool/probe_cloudreve.mjs` 是可选、只读的数据接口诊断；只在获得授权后手动执行。
- `tool/android_qa.mjs` 会操作选定设备。截图保存在被忽略的 `outputs/` 中，分享前仍需脱敏。

## 发布前检查的边界

仓库检查脚本只拦截常见私人产物、个人路径和可识别的密钥格式，不等价于完整的秘密扫描、依赖漏洞分析或渗透测试。公开发布前仍需扫描完整 Git 历史，启用 GitHub secret scanning / push protection（如可用），核对锁定依赖与原生库安全公告。

如果发现真实凭据已被提交，先撤销或轮换，再处理历史；仅删除当前文件不足以使凭据失效。
