# 代码规范化检查记录

检查日期：2026-09-08。

后续通用构建配置补充：去除个人部署标识和默认站点，新增可校验的应用元数据构建命令。当前静态分析无问题，142 项 Flutter 测试与 5 项构建参数测试通过；其中覆盖首次无站点登录、服务器添加/删除和旧地址迁移。自定义元数据的分 ABI release 模式构建通过，APK 名称、包名、版本与参数一致，独立模拟器包可启动。仍使用本地测试签名，并非正式发行验收。原规范化阶段的 134 项记录保留如下，不混同为当时已经完成的新检查。

## 结论

项目已有较完整的功能与回归测试，但原先偏向个人开发环境：命名、工具版本、提交范围和发布签名缺乏统一约束。本次完成工程层面的规范化，保留既有功能与上游来源，不进行大规模业务重写。

| 项目 | 整理前 | 整理后 |
| --- | --- | --- |
| 静态分析 | 117 项提示 | 无错误、警告或提示 |
| 文件及导入 | 37 个大小写混用文件，导入风格不一 | 手写 Dart 文件统一命名、导入与格式 |
| 回归测试 | 130 项通过 | 134 项通过，补充分享协议及对话框生命周期测试 |
| 工具链 | Flutter stable 浮动、应用锁文件被忽略 | 固定 Flutter 3.47.2，锁文件进入可提交范围 |
| 发布签名 | release 自动回退调试签名 | 正式签名缺失时阻止构建；本地测试需显式允许 |
| 仓库产物 | 输出与诊断材料未完整隔离 | 日志、截图、APK、签名和本地配置被排除 |
| 协作资料 | README 过时，缺少持续检查 | README、贡献/安全说明、发布清单及 CI |

## 本次验证

- Dart 格式检查通过；`dart analyze --fatal-infos` 无问题。
- 全量 134 项单元、组件与协议测试通过。
- Android 分 ABI release 模式构建通过，使用明确允许的本地测试签名。
- 缺少正式签名时，构建按预期报错并停止。
- 仓库提交范围检查通过，未发现检查规则覆盖的私人产物、个人路径或已知密钥格式。
- 未创建 Git 提交、推送仓库或公开发布 APK。

本地验证日志保存在被忽略的 `outputs/` 中。未执行本次版本的真机验收；GitHub CI 尚未在目标仓库运行，不能把本地结果当作这些检查已完成。

## 仍需逐步改善

当前 `lib/` 有 50 个 Dart 文件，约 1.4 万行。文件页面约 1678 行、主导航约 1285 行、仓储层约 1204 行，仍存在职责集中的维护负担。后续应结合功能迭代拆分文件操作、预览协调和协议服务，避免为了拆文件而同时改变行为。

部分历史 UI 测试使用源码结构断言。此次补充的是实际组件交互测试，后续应逐步增加行为、视觉及真机集成覆盖，而不是仅增加字符串断言数量。

Android 构建仍有 Gradle 弃用 API 提示；当前固定工具链构建成功，但升级 Gradle 10 或原生插件前仍需单独检查兼容性，不能把 Dart 静态分析零问题理解为所有构建工具均无提示。

轻量仓库检查不等于完整安全审计：完整 Git 历史、依赖公告和第三方原生库许可仍需在公开发行前核对。统一 LICENSE 尚未选择；本次没有擅自追加协议。详见 [发布清单](RELEASE_CHECKLIST.md)。
